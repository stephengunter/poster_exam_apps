export const isRecruitSource = (type) => type === 0;

export const isRandomSource = (type) => type === 1;

export const isExactlyRecruit = (rtype) => rtype === 0;

export const isCrossYearsRecruit = (rtype) => rtype === 1;