export const getPlanStartDateText = (plan) => '即日起';

export const getPlanEndDateText = (plan) => plan.endDateText ? plan.endDateText : '本次郵局招考筆試結束';

export const getSubscribeStartDateText = (subscribe) => subscribe.startDateText ? subscribe.startDateText : '即日起';